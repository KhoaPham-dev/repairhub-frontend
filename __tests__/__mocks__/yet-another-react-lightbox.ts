// Test stub for yet-another-react-lightbox (ESM-only in node_modules,
// jest can't parse it). Components and plugins all become no-op exports.
const Noop = () => null;

export default Noop;
export const Zoom = Noop;
export const Download = Noop;
export const Counter = Noop;
